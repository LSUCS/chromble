{
  "name": "chromble",
  "version": "2.1.0",
  "description": "Automated web browser for the Big Screen PC at LSUCS LANs",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "pack": "electron-builder --dir",
    "dist": "electron-builder --windows --x64"
  },
  "author": "LSUVGS",
  "license": "Apache-2.0",
  "devDependencies": {
    "electron": "^1.8.6",
    "electron-builder": "^20.36.2"
  },
  "dependencies": {
    "ini": "^1.3.5"
  },
  "build": {
    "appId": "uk.org.lsuvgs.chromble"
  }
}
